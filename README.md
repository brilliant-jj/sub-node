# sub-node
1. 在 `lib.rs` 中添加转移存证相关的结构体和方法：
2. 在 `mod.rs` 中更新 Trait 和 Call 的定义，增加新的转移存证相关的函数调用：
3. 更新 `runtime/src/lib.rs` 文件中的存储类型和转移存证相关函数的调用：
4.重新编译 Substrate-node-template，启动节点，使用 Polkadot.js App 或其他的客户端实现转移存证的测试操作。
